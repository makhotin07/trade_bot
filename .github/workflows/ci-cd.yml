name: CI/CD Pipeline

on:
  push:
    branches: [ main, master, develop ]
  pull_request:
    branches: [ main, master, develop ]

jobs:
  lint:
    runs-on: ubuntu-latest
    name: Lint Code
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
    
    - name: Cache pip packages
      uses: actions/cache@v3
      with:
        path: ~/.cache/pip
        key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
        restore-keys: |
          ${{ runner.os }}-pip-
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install flake8 black isort mypy
        pip install -r requirements.txt
    
    - name: Check code formatting with Black
      run: black --check src/ tests/ || true
    
    - name: Check import sorting with isort
      run: isort --check-only src/ tests/ || true
    
    - name: Lint with flake8
      run: |
        flake8 src/ tests/ --count --select=E9,F63,F7,F82 --show-source --statistics || true
        flake8 src/ tests/ --count --exit-zero --max-complexity=10 --max-line-length=100 --statistics || true
    
    - name: Type check with mypy
      run: |
        export PYTHONPATH=${{ github.workspace }}/src:$PYTHONPATH
        mypy src/ --ignore-missing-imports || true

  test:
    runs-on: ubuntu-latest
    name: Run Tests
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install pytest pytest-cov
    
    - name: Run tests
      run: |
        export PYTHONPATH=${{ github.workspace }}/src:$PYTHONPATH
        pytest tests/ -v --cov=src/bytbit_trading_bot || true

  deploy:
    needs: [lint, test]
    runs-on: ubuntu-latest
    name: Deploy to Server
    if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Setup SSH
      uses: webfactory/ssh-agent@v0.7.0
      with:
        ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}
    
    - name: Add server to known hosts
      run: |
        mkdir -p ~/.ssh
        ssh-keyscan -H ${{ secrets.SERVER_HOST }} >> ~/.ssh/known_hosts
    
    - name: Deploy to server
      run: |
        ssh -o StrictHostKeyChecking=no ${{ secrets.SERVER_USER }}@${{ secrets.SERVER_HOST }} << 'DEPLOY_EOF'
          set -e
          cd ${{ secrets.SERVER_PATH }} || {
            echo "❌ Директория не найдена, создаем..."
            mkdir -p ${{ secrets.SERVER_PATH }}
            cd ${{ secrets.SERVER_PATH }}
          }
          
          echo "📋 Проверка git репозитория..."
          if [ ! -d ".git" ]; then
            echo "🔄 Инициализация git репозитория..."
            git init
            git branch -M master
            git remote add origin https://github.com/makhotin07/trade_bot.git || git remote set-url origin https://github.com/makhotin07/trade_bot.git
          else
            echo "✅ Git репозиторий уже существует"
            git remote set-url origin https://github.com/makhotin07/trade_bot.git || git remote add origin https://github.com/makhotin07/trade_bot.git || true
          fi
          
          echo "🔄 Обновление кода из GitHub..."
          # Убеждаемся что используется HTTPS
          git remote set-url origin https://github.com/makhotin07/trade_bot.git || true
          
          # Безопасное обновление без удаления локальных файлов
          git fetch origin master || {
            echo "⚠️  Fetch не удался"
            exit 1
          }
          
          # Пытаемся обновить код, но не удаляем локальные изменения
          if git pull origin master 2>/dev/null; then
            echo "✅ Код успешно обновлен через pull"
          else
            echo "⚠️  Pull не удался, пробуем merge..."
            git merge origin/master --no-edit || {
              echo "⚠️  Merge не удался, но сохраняем локальные файлы"
              # Отменяем merge, но сохраняем локальные файлы
              git merge --abort 2>/dev/null || true
            }
          fi
          
          echo "✅ Код обновлен. Текущий коммит:"
          git log --oneline -1 || echo "⚠️  Не удалось получить коммит"
          
          echo "📦 Обновление зависимостей..."
          if [ -d ".venv" ]; then
            source .venv/bin/activate
          elif [ -d "venv" ]; then
            source venv/bin/activate
          else
            echo "⚠️  Виртуальное окружение не найдено, создаем..."
            python3 -m venv .venv
            source .venv/bin/activate
          fi
          
          pip install -r requirements.txt --upgrade --quiet || {
            echo "⚠️  Ошибка установки зависимостей"
          }
          
          echo "🔄 Перезапуск бота..."
          if systemctl is-active --quiet bytbit-bot.service 2>/dev/null; then
            systemctl restart bytbit-bot.service
            echo "✅ Бот перезапущен через systemd"
          elif [ -f "scripts/restart.sh" ]; then
            bash scripts/restart.sh
            echo "✅ Бот перезапущен через скрипт"
          else
            echo "⚠️  Бот не запущен. Запустите вручную: python main.py"
          fi
          
          echo "✅ Деплой завершен успешно"
        DEPLOY_EOF
